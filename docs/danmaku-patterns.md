# 弾幕パターン (35 種)

すべて `src/game/patterns.ts` の `firePattern(patternId, ctx)` の switch case として実装されている。
日本語ラベルは `PATTERN_DESCRIPTIONS` を参照。

## 一覧

| ID | 名称 | 概要 |
|---|---|---|
| 1 | 扇状弾幕 (5-way) | プレイヤー方向へ 5 発を扇形に |
| 2 | 円形弾幕 (8 方向) | 全方位 8 等分 |
| 3 | 円形弾幕 (16 方向) | 全方位 16 等分 |
| 4 | 円形弾幕 (24 方向) | 全方位 24 等分 |
| 5 | 円形弾幕 (32 方向) | 全方位 32 等分 (密集) |
| 6 | シングルスパイラル | 1 本の螺旋。enemy ごとに角度オフセットを保持 |
| 7 | ダブルスパイラル | 2 本の螺旋 |
| 8 | トリプルスパイラル | 3 本の螺旋 |
| 9 | 波状弾幕 | サイン波軌道の 5 発 (`wave` フラグ) |
| 10 | ランダム弾幕 | 角度・速度ランダム 10 発 |
| 11 | 収束弾幕 | 画面中央へ徐々に旋回 (`converging`) |
| 12 | 拡散弾幕 | 画面中央から離れる方向へ徐々に旋回 (`diverging`) |
| 13 | 十字型 | 上下左右の 4 方向 |
| 14 | X 字型 | 斜め 4 方向 |
| 15 | 米字型 | 8 方向 (十字 + X) |
| 16 | ホーミング弾 | 3 発、即時ホーミング (`homing` + `homingDelay=0`) |
| 17 | 加速弾 | プレイヤー狙い 5 発、徐々に加速 (`accelerating`) |
| 18 | 減速弾 | プレイヤー狙い 5 発、徐々に減速 (`decelerating`) |
| 19 | 二段階弾幕 | 4 方向、500ms 後にプレイヤーへ方向転換 (`twoStage`) |
| 20 | 時間差弾幕 | 100ms ずつ時間差で 5 発連射 |
| 21 | 密集弾幕 | プレイヤー方向へ狭角で 20 発 |
| 22 | まばら弾幕 | プレイヤー方向へ広角で 4 発 |
| 23 | 扇形集中 | 狭い角度で 7 発 |
| 24 | 扇形分散 | 広い角度で 7 発 |
| 25 | ダブル円形 | 異なる速度で 2 重の円形弾幕 |
| 26 | トリプル円形 | 異なる速度で 3 重の円形弾幕 |
| 27 | バースト弾 | 3 回に分けて 8 方向へバースト (150ms 間隔) |
| 28 | ストリーム弾 | 50ms 間隔で 10 発を高速発射 |
| 29 | 直線自機狙い | プレイヤーへ単発直線 |
| 30 | 予測弾 | プレイヤーの移動を予測して 3 発 |
| 31 | 爆発型 | 16 方向へランダム速度で爆発的に |
| 32 | 回転弾幕 (時計回り) | 6 方向が時計回りに回転 |
| 33 | 回転弾幕 (反時計回り) | 6 方向が反時計回りに回転 |
| 34 | 花弁状 | 5 つの花弁形状で 15 発 |
| 35 | ランダムホーミング | ランダム方向で発射、500-1000ms 後にホーミング開始 |

## パターン追加手順

1. `src/game/patterns.ts` の `firePattern` の `switch` に新しい `case` を追加
2. `PATTERN_DESCRIPTIONS` に日本語ラベルを追加
3. `PATTERN_COUNT` を増やす (`src/scenes/GameScene.ts` の敵スポーン時のランダム選択範囲が連動)
4. 必要なら `BulletFlags` を拡張し、`applyBulletBehaviors()` に挙動を実装

## 弾の挙動フラグ

| flag | 適用 (`applyBulletBehaviors`) |
|---|---|
| `homing` | `homingDelay` 経過後、プレイヤーへ `0.05 rad/frame` で旋回 |
| `accelerating` | `vx,vy *= 1 + 0.002 * dtMs` |
| `decelerating` | `vx,vy *= 1 - 0.002 * dtMs` |
| `wave` | `wavePhase += 0.1`、進行角度に `sin(phase) * 0.05` を加算 |
| `converging` | 画面中央 (400, 300) へ `0.02 rad/frame` で旋回 |
| `diverging` | 画面中央から離れる方向へ `0.02 rad/frame` で旋回、速度 1.005× |
| `twoStage` | `stageSwitchAt` 経過後、プレイヤー方向 (速度 250) に方向転換 |
